我需要为达芬奇剪辑软件开发插件，需要实现以下核心功能：
1.批量导出时间线功能，自动以媒体池结构显示，例如用户的时间线放在了“工程”文件夹中，那么在插件里选择对应的文件夹后，自动显示该文件夹里面的全部时间线，然后点选或者shift多选需要导出的时间线，点击导出后，自动将这些时间线按照用户设置的分辨率、帧率等参数进行批量导出，导出设置面板严格按照达芬奇的导出设置来设计，用户可以选择导出格式、编码器、分辨率、帧率等各项高级参数，支持自定义命名规则，例如可以在文件名前添加时间线名称、日期等信息。
以及导出文件夹等设置，用户可以选择导出文件夹的路径

需要支持最新版19以上的达芬奇剪辑软件

这个项目的源代码和各项产物均存放在“批量导出”文件夹内进行，科学合理的文件夹结构存放源代码、文档、测试用例等内容，确保项目的可维护性和可扩展性。