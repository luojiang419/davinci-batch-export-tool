# 进度快照 86 - v1.1.20多段音频合板版本发布完成

## 版本信息
- 新版本号: `v1.1.20`
- `pubspec.yaml`: `1.1.20+38`
- 阶段备份: `backup/v0.43.0`
- 发布目录: `dist/v1.1.20`
- Git 分支: `main`

## 已完成内容

### 1. 已完成多段音频合板主链路发布
- 自动合板主流程已从“单音频片段”升级为“总字幕对齐 + 多段源音频回落”
- 已支持：
  - generatedAggregate 总字幕兜底
  - 多段音频 segment 持久化
  - 多段 XML/FCPXML 外部音频导出
  - CSV 诊断字段补充
  - 时间线预览显示 segment

### 2. 已完成数据库升级与兼容
- 数据库版本已升级到：
  - `6`
- 已新增：
  - `sync_audio_segments` 表
- 正式包空数据库已确认：
  - `PRAGMA user_version = 6`
  - `projects = 0`
  - `sync_audio_segments` 表存在

### 3. 已完成测试与真实基准
- 已通过：
  - `flutter test`
- 已补新增测试：
  - `generatedAggregate` 自动生成
  - 无假越界单音频对齐
  - 多段音频切分与接续
  - 多段 xmeml/fcpxml 导出
- 已实际执行 0916 字幕真实基准脚本并生成：
  - `测试合板/220822_real_sync_benchmark.xml`
  - `测试合板/220822_real_sync_benchmark.fcpxml`
  - `测试合板/220822_real_sync_benchmark.csv`
  - `测试合板/220822_real_sync_benchmark.segments.txt`

### 4. 0916 基准结果已明显收口
- 当前结果分布：
  - `10` 条中置信度
  - `1` 条音频过短
- `segment_count` 分布：
  - `1` 段: `6` 条
  - `2` 段: `3` 条
  - `3` 段: `1` 条
  - `4` 段: `1` 条
- 起点不足提示已降到：
  - `1` 条
- 与改造前相比：
  - 已不再出现大量同文件细碎切片
  - 录音机重启类视频已能自动跨文件接续

### 5. 已完成 Windows release 构建与清洁发布
- 已执行：
  - `flutter build windows --release`
  - `python3 tool/prepare_clean_release.py dist/v1.1.20`
- 已确认发布目录存在：
  - `dist/v1.1.20/asr_tools.exe`
  - `dist/v1.1.20/sqlite3.dll`
  - `dist/v1.1.20/flutter_windows.dll`
  - `dist/v1.1.20/desktop_drop_plugin.dll`
  - `dist/v1.1.20/window_manager_plugin.dll`
  - `dist/v1.1.20/data/config/asr_tools_settings.json`
  - `dist/v1.1.20/data/database/asr_tools.db`
  - `dist/v1.1.20/data/projects`
  - `dist/v1.1.20/data/temp`

### 6. 已完成临时副本启动冒烟
- 已基于 `dist/v1.1.20` 复制临时副本启动验证
- 应用可正常拉起并退出
- 正式发布目录未被启动验证污染

## 当前修改到哪个模块
- 字幕反解与 generatedAggregate
- 自动合板主流程
- 数据库与 segment 持久化
- 时间线构建
- XML/FCPXML/CSV 导出
- 时间线预览与复核只读保护
- 0916 真实基准脚本

## 验证结果
- `flutter test` 全量通过
- `flutter build windows --release` 通过
- `dist/v1.1.20/data/database/asr_tools.db`
  - `user_version = 6`
  - `projects = 0`
  - `sync_audio_segments` 表存在
- 临时副本启动冒烟通过
- 0916 真实基准已执行完成并输出新的 XML/FCPXML/CSV/segments

## 待办清单
- [ ] 如需要，可继续把真实基准脚本改成真正零退出码的一键验收入口
- [ ] 如需要，可继续把多段结果在复核弹窗里做成更完整的 segment 可视化

## 下一步
- 可以直接使用 `dist/v1.1.20` 验证多段音频合板。
- 如果你要我继续追 0916 基准里唯一剩下的 `音频过短` 样例，我会直接针对那条继续收窄算法。
