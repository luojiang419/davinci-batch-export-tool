# 进度快照 90 - v1.1.22一键合板XML错位修复版本发布完成

## 版本信息
- 新版本号: `v1.1.22`
- `pubspec.yaml`: `1.1.22+40`
- 阶段备份: `backup/v0.44.0`
- 发布目录: `dist/v1.1.22`
- Git 分支: `main`

## 已完成内容

### 1. 已修复 XML 导出与复核结果可能不一致的问题
- 导出 XML 前现在会强制重建最新时间线
- 复核页刚接受或手动纠偏后的结果不再受旧缓存影响

### 2. 已增强 xmeml 对 Premiere 的 source in/out 表达
- 视频、内嵌音频、外录音频 `clipitem` 现已写出：
  - `masterclipid`
  - `pproTicksIn`
  - `pproTicksOut`
- 这样长时间线导入时，Premiere 拿到的是更明确的源片段定位信息

### 3. 已补回归测试并通过
- 已扩展：
  - `test/export_service_test.dart`
- 已确认：
  - Premiere ticks 与 `in/out` 帧值严格对应
  - 共享音频文件定义仍正确复用
  - 全量测试通过

### 4. 已完成 Windows release 构建与清洁发布
- 已执行：
  - `flutter build windows --release`
  - `python3 tool/prepare_clean_release.py dist/v1.1.22`
- 已确认发布目录存在：
  - `dist/v1.1.22/asr_tools.exe`
  - `dist/v1.1.22/sqlite3.dll`
  - `dist/v1.1.22/flutter_windows.dll`
  - `dist/v1.1.22/desktop_drop_plugin.dll`
  - `dist/v1.1.22/window_manager_plugin.dll`
  - `dist/v1.1.22/data/config/asr_tools_settings.json`
  - `dist/v1.1.22/data/database/asr_tools.db`
  - `dist/v1.1.22/data/projects`
  - `dist/v1.1.22/data/temp`

## 当前修改到哪个模块
- XML 导出前时间线刷新
- xmeml Premiere 兼容字段输出
- 导出回归测试
- Windows 发布目录整理

## 验证结果
- `flutter test test/export_service_test.dart` 通过
- `flutter test` 全量通过
- `dist/v1.1.22/data/database/asr_tools.db`
  - `user_version = 6`
  - `projects = 0`

## 待办清单
- [ ] 等你用 `dist/v1.1.22` 实机导入 XML 复测 `C0061 / ZOOM0230`
- [ ] 如 Premiere 仍出现偏移，再继续补更细的 Premiere 专有 XML 结构

## 下一步
- 本轮代码、快照和版本文件提交并推送到 GitHub
- 你可以直接用 `dist/v1.1.22` 重新导出并复测 XML
